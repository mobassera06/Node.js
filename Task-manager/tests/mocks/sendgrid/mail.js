const e = require("express")

//mocking libraries
// manual mocks are used to stub out the functionality
// with mock data,it allows to use fake data to ensure test is fast
// __mocks__/subdirectory(case sensitive)
// e.g -to mock a module called user in models directry,create a file
// called user.js and put in models/__mock__ dir
module.exports = {
    setApiKey() {

    },
    send(){

    }
}